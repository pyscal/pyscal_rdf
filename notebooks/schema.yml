material:
  element_ratio:
    element1: float
  crystal_structure:
    altname: string
    spacegroup_symbol: string
    spacegroup_number: int
    unit_cell:
      bravais_lattice: string
      lattice_parameter: float(3)
      angle: float(3)
  
  defect:
    grain_boundary:
      type: string
      sigma: int
      plane: int(3)
      rotation_axis: int(3)
      misorientation_angle: float
    vacancy:
      type: string

simulation_cell:
  volume: float
  number_of_atoms: int
  length: float(3)
  vector: float(float(3))
  angle: float(3)
  vacancy_concentration: float
  number_of_vacancies: int

calculated_property:
  - label: string
    value: float
    unit: string

